/**
* Timer class handles text segments tab timings creation/ovverride.
* When the text is in playing, and the user start to tab(press specific keyboard keys), the current segment's timing slot is overriden by the current time (the time between the current key press and the previous one or the start time if it's the first tab) 
*/
Timer = Class.$extend({
  /**
  * Initilize class variables (classy.js is used for class creation)
  */
  __init__: function() {
  }
});